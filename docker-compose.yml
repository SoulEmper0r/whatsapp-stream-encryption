name: whatsapp-stream-encryption

services:
  app:
    build: .
    working_dir: /app
    command: ["sh", "-c", "while true; do sleep 3600; done"]
    volumes:
      - .:/app
      - vendor:/app/vendor

volumes:
  vendor:

